(Phase 1 working notes)

- Minute input currently persists `liveMatch.minute` string; public scoreboard renders that string.
- Phase 1 will replace minute string with countdown driven by `countdownEndsAt` and phase.
- Half durations required: Half 1 = 20m, Half 2 = 10m.

Planned LiveMatch fields:
- phase: 'HALF_1' | 'HALF_2' | 'PAUSED' | 'NOT_STARTED'
- countdownEndsAt: Date | null
- countdownDurationSeconds: number
- isLive: boolean (existing)

